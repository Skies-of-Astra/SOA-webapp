/**
 *
 * @param {{cssClass:string, content:string}} props
 */

const Paragraph = (props) => {
  return (
    <p key="ghh*&&" className={props.cssClass}>
      {props.children}
    </p>
  );
};

export default Paragraph;
